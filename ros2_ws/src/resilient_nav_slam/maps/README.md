# Phase 9 maps

`phase9/occupancy/` contains the persisted healthy Occupancy Map baseline, and
`phase9/posegraph/` contains the corresponding Slam Toolbox serialized
pose-graph files. `phase9/metadata.yaml` records their experiment provenance.

These assets contain no Ground Truth export or benchmark output. Reload,
localization, and evaluator use remain separately authorized work.
